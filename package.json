{
  "name": "md-links",
  "version": "0.1.0",
  "devDependencies": {
    "eslint": "^5.16.0",
    "jest": "^24.7.1"
  },
  "scripts": {
    "eslint": "eslint --ext .js ./test/",
    "pretest": "npm run eslint",
    "test": "jest"
  },
  "dependencies": {
    "chalk": "^2.4.1",
    "link-check": "^4.4.4"
  }
}
